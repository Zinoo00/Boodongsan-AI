# 데이터 로딩 가이드

국토교통부 및 서울시 공공 데이터를 LightRAG에 로딩하는 방법을 설명합니다.

## 📋 목차

1. [사전 준비](#사전-준비)
2. [CLI 사용법](#cli-사용법)
3. [API 사용법](#api-사용법)
4. [데이터 소스](#데이터-소스)
5. [문제 해결](#문제-해결)

---

## 사전 준비

### 1. API 키 설정

`.env` 파일에 필수 API 키를 설정해야 합니다:

```bash
# 국토교통부 실거래가 API (필수)
MOLIT_API_KEY=your_molit_api_key_here

# Anthropic Claude API (필수)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# 서울시 열린 데이터 광장 (선택)
SEOUL_OPEN_API_KEY=your_seoul_api_key_here
```

### 2. API 키 발급 방법

#### 국토교통부 API 키
1. [공공데이터포털](https://www.data.go.kr/) 접속
2. 회원가입 및 로그인
3. "실거래가 정보" 검색
4. 활용신청 후 승인 대기 (보통 1-2시간)

#### Anthropic API 키
1. [Anthropic Console](https://console.anthropic.com/) 접속
2. API Keys 메뉴에서 새 키 생성

### 3. 환경 확인

데이터 로딩 전 환경을 확인합니다:

```bash
cd backend
uv run python -m scripts.load_data check
```

출력 예시:
```
✅ 국토교통부 API 키: 설정됨
✅ Anthropic API 키: 설정됨
시군구 데이터: 250개 로드됨
✅ 국토교통부 API 연결 성공
```

---

## CLI 사용법

### 1. 샘플 데이터 로딩 (추천)

처음 사용하거나 테스트할 때는 샘플 데이터를 먼저 로딩합니다:

```bash
uv run python -m scripts.load_data load --mode sample
```

**샘플 데이터 내용:**
- 서울시 전체 시군구 행정구역 정보 (~250개)
- 강남구, 서초구, 송파구 아파트 전월세 데이터 (최대 500건)
- ⚠️ **예상 소요 시간: 1-2시간** (LightRAG의 깊은 분석 때문에 느림)

### 2. 소량 테스트 (추천)

실제 데이터 로딩 전에 소량으로 먼저 테스트:

```bash
# 강남구 데이터 10개만 테스트
uv run python -m scripts.load_data load --mode full --districts 강남구 --limit 10

# 예상 소요 시간: 2-3분
```

### 3. 전체 데이터 로딩

⚠️ **경고: LightRAG는 매우 느립니다!**
- 처리 속도: 분당 4-6개 문서
- 1000개 문서 = **3-4시간 소요**
- 전체 데이터 (수만 건) = **수일 소요 가능**

```bash
# 전체 자치구 데이터 수집 (권장하지 않음)
uv run python -m scripts.load_data load --mode full

# 특정 자치구 + 제한 (권장)
uv run python -m scripts.load_data load --mode full --districts 강남구 --limit 1000
```

### 4. 특정 자치구만 로딩

원하는 자치구만 선택해서 데이터를 수집할 수 있습니다:

```bash
# 강남3구만 수집 (제한 없이 전체)
uv run python -m scripts.load_data \
  load --mode full \
  --districts 강남구,서초구,송파구
```

### 5. 특정 시점 데이터 로딩

과거 특정 월의 데이터를 수집할 수 있습니다:

```bash
# 2024년 10월 데이터 수집
uv run python -m scripts.load_data \
  load --mode full \
  --year-month 202410 \
  --districts 강남구 \
  --limit 100
```

### 6. 명령어 옵션

```bash
uv run python -m scripts.load_data --help
```

**주요 옵션:**
- `load --mode` / `-m`: 로딩 모드 (`sample` 또는 `full`)
- `--districts` / `-d`: 수집할 자치구 (쉼표로 구분)
- `--year-month` / `-ym`: 수집 기준 연월 (YYYYMM 형식)
- `--limit` / `-l`: 최대 수집 레코드 수 (테스트용)

---

## API 사용법

FastAPI 서버를 통해 데이터를 로딩할 수도 있습니다.

### 1. 서버 시작

```bash
cd backend
uv run uvicorn api.main:app --reload
```

### 2. 샘플 데이터 로딩

```bash
curl -X POST "http://localhost:8000/api/v1/admin/load-data" \
  -H "Content-Type: application/json" \
  -d '{"mode": "sample"}'
```

응답:
```json
{
  "status": "started",
  "message": "데이터 로딩 시작됨 (모드: sample)",
  "started_at": "2024-10-31T14:30:00"
}
```

### 3. 진행 상황 확인

```bash
curl "http://localhost:8000/api/v1/admin/status"
```

응답:
```json
{
  "is_loading": true,
  "last_load_time": "2024-10-31T14:30:00",
  "last_stats": null
}
```

### 4. 사용 가능한 자치구 확인

```bash
curl "http://localhost:8000/api/v1/admin/districts"
```

### 5. 데이터 통계 확인

```bash
curl "http://localhost:8000/api/v1/admin/stats"
```

### 6. 데이터 완전 삭제 (주의!)

```bash
curl -X DELETE "http://localhost:8000/api/v1/admin/clear-data?confirm=true"
```

---

## 데이터 소스

### 1. 국토교통부 실거래가 데이터

**출처:** [공공데이터포털](https://www.data.go.kr/)

**수집 데이터:**
- 아파트 매매 실거래가
- 아파트 전월세 실거래가
- 단독/다가구 매매 및 전월세 (계획 중)
- 오피스텔 매매 및 전월세 (계획 중)

**데이터 필드:**
- 위치 정보: 시도, 시군구, 읍면동, 지번, 도로명
- 부동산 정보: 건물명, 전용면적, 층수, 건축년도
- 거래 정보: 거래금액, 보증금, 월세, 거래일자
- 메타 정보: 데이터 수집 시각, 출처

**업데이트 주기:**
- 국토교통부: 월 1회 (매월 말 익월 초 업데이트)
- 수집 권장: 월 1회

### 2. 시군구 행정구역 데이터

**출처:** 내부 데이터셋 (`backend/data/collectors/sample-data/sigungu.json`)

**수집 데이터:**
- 전국 시도/시군구/읍면동 코드 및 명칭
- 총 ~250개 시군구 정보

### 3. 서울시 열린데이터 광장 (선택)

**출처:** [서울 열린데이터 광장](https://data.seoul.go.kr/)

**수집 가능 데이터 (향후 확장):**
- 실시간 지하철 혼잡도
- 공공시설 위치 정보
- 교통/환경 데이터

---

## LightRAG 데이터 구조

### 데이터 변환 과정

1. **구조화된 데이터 수집** (JSON)
   ```json
   {
     "property_type": "아파트",
     "transaction_type": "매매",
     "address": "서울특별시 강남구 역삼동",
     "price": 150000000,
     "area_m2": 84.5
   }
   ```

2. **자연어 문서로 변환**
   ```
   아파트 매매 정보
   위치: 서울특별시 강남구 역삼동
   매매가: 1,500,000,000원
   전용면적: 84.50㎡ (25.6평)
   데이터 출처: MOLIT
   ```

3. **LightRAG 지식 그래프 구축**
   - 자동 엔티티 추출 (지역, 가격, 면적 등)
   - 관계 추출 (위치-매물, 가격-면적 등)
   - 벡터 임베딩 생성 및 저장

### 저장 위치

```
backend/lightrag_storage/BODA/
├── vdb_entities.json          # 엔티티 벡터 DB
├── vdb_relationships.json     # 관계 벡터 DB
├── graph_chunk_entity_relation.graphml  # 지식 그래프
└── ...
```

---

## 문제 해결

### 1. API 키 오류

**증상:**
```
❌ 국토교통부 API 키: 미설정
```

**해결:**
1. `.env` 파일 확인
2. `MOLIT_API_KEY` 값이 올바르게 설정되었는지 확인
3. 서버 재시작 후 다시 시도

### 2. API 연결 실패

**증상:**
```
❌ 국토교통부 API 연결 실패
```

**해결:**
1. API 키 활용 신청 승인 여부 확인
2. 네트워크 연결 상태 확인
3. API 키 유효기간 확인

### 3. 타임아웃 오류

**증상:**
```
ERROR: Request timed out
```

**해결:**
1. 수집 대상 자치구 수를 줄임 (`--districts` 옵션 사용)
2. 샘플 모드로 먼저 테스트
3. 네트워크 안정성 확인

### 4. 메모리 부족

**증상:**
```
MemoryError: Unable to allocate array
```

**해결:**
1. 샘플 모드 사용 (`load --mode sample`)
2. 자치구를 나누어 여러 번에 걸쳐 수집
3. 시스템 메모리 증설 고려

### 5. LightRAG 초기화 실패

**증상:**
```
Failed to initialize LightRAG
```

**해결:**
1. 작업 디렉토리 권한 확인
2. 기존 데이터 삭제 후 재시도:
   ```bash
   rm -rf backend/lightrag_storage/BODA
   ```
3. Anthropic API 키 확인

---

## 성능 최적화

### 1. 배치 크기 조정

대량 데이터 수집 시 배치 크기를 조정하여 성능을 최적화할 수 있습니다.

`backend/data/collectors/real_estate_collector.py`:
```python
DEFAULT_PAGE_SIZE = 1000  # 기본값
# 네트워크가 느린 경우: 500
# 네트워크가 빠른 경우: 2000
```

### 2. Rate Limiting

API 호출 제한을 피하기 위한 딜레이 조정:

```python
self.request_delay = 0.5  # 기본 0.5초
# 더 빠르게: 0.2초
# 더 안정적으로: 1.0초
```

### 3. 병렬 처리

향후 여러 자치구를 병렬로 수집하도록 개선 예정입니다.

---

## 성능 및 주의사항

### LightRAG의 특성

**LightRAG는 매우 느립니다.** 이는 버그가 아니라 설계상의 특징입니다:

#### 왜 느린가?

1. **깊은 분석**: 각 문서마다 엔티티와 관계를 추출 (LLM 호출)
2. **지식 그래프 구축**: 추출된 정보를 그래프로 연결
3. **중복 제거**: 기존 엔티티와 병합 (추가 LLM 호출)
4. **디스크 쓰기**: 매번 전체 그래프를 저장

#### 실제 성능

| 항목 | 속도 |
|------|------|
| 처리 속도 | 분당 4-6개 문서 |
| 100개 문서 | 약 20-25분 |
| 1000개 문서 | 약 3-4시간 |
| 10000개 문서 | 약 1-2일 |

#### 병목 요인

- **Anthropic API 호출**: 각 문서당 2-3회 호출
- **Rate Limiting**: API 제한으로 인한 재시도
- **순차 처리**: 문서를 하나씩 처리

### 권장 사항

1. **소량 테스트부터 시작**
   ```bash
   uv run python -m scripts.load_data load --mode full --districts 강남구 --limit 10
   ```

2. **제한적 데이터 수집**
   - 전체 데이터 대신 특정 자치구만
   - `--limit` 옵션으로 개수 제한

3. **백그라운드 실행**
   ```bash
   nohup uv run python -m scripts.load_data load --mode full --districts 강남구 --limit 1000 > load.log 2>&1 &
   ```

4. **진행상황 모니터링**
   ```bash
   tail -f load.log
   ```

### 대안

만약 LightRAG가 너무 느리다면:

1. **Simple Vector DB**: ChromaDB, Pinecone 등 (빠르지만 단순)
2. **Batch Processing**: 오프라인에서 대량 처리 후 API 제공
3. **Pre-built Index**: 미리 구축된 인덱스 사용

---

## 다음 단계

데이터 로딩 후:

1. **API 테스트**
   ```bash
   curl "http://localhost:8000/api/v1/chat/send" \
     -H "Content-Type: application/json" \
     -d '{
       "message": "강남구 아파트 시세 알려줘",
       "user_id": "test_user",
       "conversation_id": null
     }'
   ```

2. **프론트엔드 연결**
   ```bash
   cd frontend
   streamlit run app.py
   ```

3. **정기 업데이트 설정**
   - Cron job 또는 스케줄러를 사용하여 월 1회 자동 업데이트 설정

---

## 참고 자료

- [국토교통부 실거래가 API 문서](https://www.data.go.kr/dataset/3050988/openapi.do)
- [LightRAG 공식 문서](https://github.com/HKUDS/LightRAG)
- [프로젝트 README](../../README.md)
